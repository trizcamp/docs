---
title: 'Get a list of prompt names'
description: Endpoint to Get a list of prompt names with versions and labels
api: 'GET /api/public/v2/prompts'
---
<Expandable title="Request">
<ResponseField name="name" type="string">
  Optional name filter (nullable).
</ResponseField>
<ResponseField name="label" type="string">
  Optional label filter (nullable).
</ResponseField>
<ResponseField name="tag" type="string">
  Optional tag filter (nullable).
</ResponseField>
<ResponseField name="page" type="integer">
  Page number, starts at 1 (nullable).
</ResponseField>
<ResponseField name="limit" type="integer">
  Limit of items per page (nullable).
</ResponseField>
<ResponseField name="fromUpdatedAt" type="date-time">
  Optional filter for updates on or after a certain datetime (ISO 8601).
</ResponseField>
<ResponseField name="toUpdatedAt" type="date-time">
  Optional filter for updates before a certain datetime (ISO 8601).
</ResponseField>
</Expandable>

## Response Description 
Below you will see the description of the create dataset response

<ResponseField name="data" type="array" required>
  List of records.
  <Expandable title="properties">
    <ResponseField name="name" type="string" required>
      Name of the record.
    </ResponseField>
    <ResponseField name="versions" type="array" required>
      List of versions associated with the record.
      <Expandable title="items">
        <ResponseField name="version" type="integer" required>
          A specific version number.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="labels" type="array" required>
      List of labels associated with the record.
      <Expandable title="items">
        <ResponseField name="label" type="string" required>
          A specific label.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="tags" type="array" required>
      List of tags associated with the record.
      <Expandable title="items">
        <ResponseField name="tag" type="string" required>
          A specific tag.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="lastUpdatedAt" type="date-time" required>
      Timestamp of the last update for the record.
    </ResponseField>
    <ResponseField name="lastConfig" type="null">
      Last configuration details (nullable).
    </ResponseField>
  </Expandable>
</ResponseField>
<ResponseField name="meta" type="object" required>
  Pagination metadata.
  <Expandable title="properties">
    <ResponseField name="page" type="integer" required>
      Current page number.
    </ResponseField>
    <ResponseField name="limit" type="integer" required>
      Maximum number of items per page.
    </ResponseField>
    <ResponseField name="totalItems" type="integer" required>
      Total number of items available.
    </ResponseField>
    <ResponseField name="totalPages" type="integer" required>
      Total number of pages available.
    </ResponseField>
  </Expandable>
</ResponseField>

### Code Response
Exemples of Response Code 

<ResponseField name="200">
  Request Successful Response
</ResponseField>

<ResponseField name="400">
  Request Failure
</ResponseField>
